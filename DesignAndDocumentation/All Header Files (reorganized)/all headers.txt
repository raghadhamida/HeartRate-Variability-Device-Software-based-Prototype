//3004 Project Header files
//Anna Alchoujian
//from Mar 30

class PulseReading { //responsible for reading the pulse itself
private:
	bool isActive;
public:
	void startReading();
	void stopReading(); 
	void setIndicator();
};


class User { //this is the user interface
private:
	bool isDeviceOn;
	bool isSessionOn; 
	string[] menuOptions;
public:
	void on(); 
	void off(); 
	void displayMenu(); 
	void displayGraph(); 
	void selectMenuOption();
	void selectArrowButton();
};


class CoherenceIndicator { //the coherence part
private:
	int coherenceLevel;
public:
	void setLevel();
	void setLED();
};


class Session {
private:
	int[] metrics; //to store the metrics
	int duration; 
	int challengeLevel; 
	int interval; //the breath pacer interval
public:
	void start(); //(these two funcs are-
	void end(); //-for starting and ending session)
	void updateMetrics(); 
	void setChallengeLevel(); 
	void setInterval(); 
	void getSummary();
};


class History {
private:
	Session[] sessions; //stores the sessions (obj's)
public:
	void addSession(); 
	void deleteSession(); 
	void getSessions();
};

//TOTAL: 5 header files
